<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yczuoxin.springboot.mappers.primary.UserDao">
    <select id="selectById" resultType="com.yczuoxin.springboot.model.User">
        select * from User where id = #{id}
    </select>
</mapper>